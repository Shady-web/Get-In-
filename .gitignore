# Secrets — never commit these
.env
.env.local
.env.*.local

# Solana keypairs the setup scripts generate (TxLINE + devnet house float)
.txline-keypair.json
.house-keypair.json
*.keypair.json

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Next.js
.next/
out/
next-env.d.ts

# TypeScript incremental build cache
tsconfig.tsbuildinfo
